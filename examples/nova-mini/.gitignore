node_modules/
.wrangler/
.dev.vars
.dev.vars.*

# Backup written by `npm run setup`
wrangler.jsonc.bak

# dist/worker.single.js عمداً کامیت می‌شود تا بشود مستقیم در
# داشبورد کلودفلر کپی/پیست کرد. با «npm run bundle» بازتولید می‌شود.
