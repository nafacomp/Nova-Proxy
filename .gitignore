.env
.env.*
.dev.vars
.dev.vars.*
.wrangler/
node_modules/
.DS_Store
*.cpuprofile

# The example file is published on purpose: Cloudflare reads it during a
# Deploy to Cloudflare to decide which secrets to prompt for. Real secrets
# still stay out via the .dev.vars rules above.
!.dev.vars.example

# Local readability aids for auditing worker.js. Generated, never deployed.
# Regenerate with: npm run readable
analysis/

# Backup written by `npm run setup`
wrangler.jsonc.bak
